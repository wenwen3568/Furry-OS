#!/bin/bash -e
# 启用 32 位架构（Ubuntu 源里的 wine 需要）
dpkg --add-architecture i386

# 更新源
apt-get update

# 安装所需软件包（全部来自 Ubuntu 官方源）
apt-get install -y \
    calamares \
    gnome-tweaks \
    wine

# 清理缓存
apt-get clean
